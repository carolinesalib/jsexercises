function reverse(array) {
    // TODO: reverse the array and return the result
    // Avoid using .reverse() js function since the idea is to practice your logic
    return array;
}

module.exports = reverse;
